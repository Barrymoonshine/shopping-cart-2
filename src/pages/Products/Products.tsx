import './Products.css';

const Products = () => {
  // Get data from FakeStore API
  // Use data to populate Product Cards
  // Perform type checking
  return <main>This is the products page</main>;
};

export default Products;
